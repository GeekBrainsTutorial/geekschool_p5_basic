function setup() {
	createCanvas(windowWidth, windowHeight)
	background('lightblue')
	let centerX = width/2
	let centerY = height/2
	fill('red')
	rectMode(CENTER)
	rect(centerX, centerY, 200, 100)
}
function draw() {
	
}